<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<header>
    <% include ./partials/_header %>
</header>
<body>
  <table>
  <tr>
    <th>Unique ID</th>
    <th>Original Link</th>
    <th>Delete</th>
  </tr>
  <% for (const everyLink in urls) { %>
  <tr>
    <td><%= everyLink%></td>
    <td><a href=<%= urls[everyLink]%>><%= urls[everyLink]%></a></td>
    <td><form action="/urls/<%=everyLink%>/delete" method="post"><button type="submit"s>DELETE 🗑️</button></form></td>
  </tr>
  <% }; %>
  </table>
    <a href="/urls/new">Link Shortener</a>
  </ul>
</body>
</html>